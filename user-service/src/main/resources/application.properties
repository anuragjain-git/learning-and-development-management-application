spring.application.name=user-service
server.port=8081

spring.datasource.url=jdbc:mysql://localhost:3306/userdb
spring.datasource.username=root
spring.datasource.password=zsaxdwecq

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Tells Spring to wait for JPA/Hibernate to finish creating the schema (tables) before running data.sql
spring.jpa.defer-datasource-initialization=true

# Tells Spring to always run data.sql, even if you're using an external database like MySQL
spring.sql.init.mode=always

# Eureka client
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/


